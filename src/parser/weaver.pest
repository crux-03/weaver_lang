// 
// weaver-lang grammar
// 
// Two layers: template (top-level, free-form text with embedded constructs)
// and expression (typed values inside constructs).
// 
// Template parsing is NOT whitespace-insensitive. Raw text matters.
// Expression parsing (inside delimiters) IS whitespace-insensitive.

// ── Top-level ───────────────────────────────────────────────────────────

template        = { SOI ~ node* ~ EOI }
expr_standalone = { SOI ~ ws ~ expr ~ ws ~ EOI }

node = _{
    if_block
  | foreach_block
  | control_stmt
  | expression_node
  | command_node
  | literal_text
}

/// Raw text — everything that isn't a weaver construct.
/// Matches one or more characters that don't start a construct.
literal_text = @{
    (!("{{" | "{#" | "@[" | "$[" | "<trigger" | "[[") ~ ANY)+
}

// ── Expression nodes (template-level, result gets string-coerced) ───────

expression_node = _{
    variable
  | processor_call
  | trigger
  | document_ref
}

// ── Command node (template-level, return value is optional) ─────────────

command_node = { command_call }

// ── Variables: {{scope:name}} or {{name}} (bare loop binding) ───────────
//
// Either form may carry a dotted path (`{{char:alice.inventory}}`,
// `{{npc.name}}`). The FIRST segment is the variable name — the part the
// host resolves, or the loop binding. Every segment after it indexes into
// the resolved value and must find an object along the way.

variable   = { "{{" ~ trim_l? ~ ws ~ (scoped_var | bare_var) ~ ws ~ trim_r? ~ "}}" }
scoped_var = { identifier ~ ":" ~ dotted_name }
bare_var   = { dotted_name }

// ── Processors: @[namespace.name(key: value, key2: value2)] ─────────────

processor_call = {
    "@[" ~ trim_l? ~ ws ~ dotted_name ~ "(" ~ ws ~ property_list? ~ ws ~ ")" ~ ws ~ trim_r? ~ "]"
}

property_list = { property ~ (ws ~ "," ~ ws ~ property)* }

property = { identifier ~ ws ~ ":" ~ ws ~ expr }

// ── Commands: $[name(arg1, arg2)] ───────────────────────────────────────

command_call = {
    "$[" ~ trim_l? ~ ws ~ identifier ~ "(" ~ ws ~ arg_list? ~ ws ~ ")" ~ ws ~ trim_r? ~ "]"
}

arg_list = { expr ~ (ws ~ "," ~ ws ~ expr)* }

// ── Triggers: <trigger id="entry-id"> or <trigger id={{var}}> ───────────

trigger = {
    "<trigger" ~ ws ~ "id" ~ ws ~ "=" ~ ws ~ trigger_id ~ ws ~ ">"
}

/// The id is either a string literal or an expression that evaluates to
/// one. Bare binary expressions are NOT allowed here because `>` would be
/// ambiguous with the closing delimiter — parenthesize instead:
/// <trigger id=({{scope:base}} + "_suffix")>
trigger_id = _{
    quoted_string
  | variable
  | processor_call
  | command_call
  | "(" ~ ws ~ expr ~ ws ~ ")"
}

// ── Documents: [[DOCUMENT_ID]] or [[{{var}}]] ───────────────────────────

document_ref = {
    "[[" ~ ws ~ document_id ~ ws ~ "]]"
}

/// Note: in EXPRESSION position (not template position), `[[{{var}}]]`
/// still parses as a nested array literal — array syntax takes precedence
/// inside `atom`. Dynamic document ids are a template-level feature.
document_id = _{
    variable
  | processor_call
  | command_call
  | "(" ~ ws ~ expr ~ ws ~ ")"
  | identifier
}

// ── Flow statements: {# break #}, {# continue #}, {# return expr #} ─────
//
// These share the control-flow delimiter because that is what they are.
// `kw_end` prevents a longer identifier from starting a match it cannot
// finish: without it `{# breakpoint #}` begins matching `break_stmt`,
// fails at `#}`, and reports an error pointing at the wrong thing.

control_stmt = _{ break_stmt | continue_stmt | return_stmt | stop_stmt }

break_stmt    = { "{#" ~ trim_l? ~ ws ~ "break" ~ kw_end ~ ws ~ trim_r? ~ "#}" }
continue_stmt = { "{#" ~ trim_l? ~ ws ~ "continue" ~ kw_end ~ ws ~ trim_r? ~ "#}" }

/// `{# return expr #}` makes `expr` the template's value, discarding the
/// output rendered before it. A bare `{# return #}` is sugar for
/// `{# return none #}`.
return_stmt = { "{#" ~ trim_l? ~ ws ~ "return" ~ kw_end ~ (ws ~ expr)? ~ ws ~ trim_r? ~ "#}" }

/// `{# stop #}` ends evaluation and keeps the output rendered so far.
stop_stmt = { "{#" ~ trim_l? ~ ws ~ "stop" ~ kw_end ~ ws ~ trim_r? ~ "#}" }

kw_end = _{ !(ASCII_ALPHANUMERIC | "_") }

// ── Control flow ────────────────────────────────────────────────────────

if_block = {
    if_open ~ node* ~ elif_branch* ~ else_branch? ~ if_close
}

if_open  = { "{#" ~ trim_l? ~ ws ~ "if" ~ ws ~ expr ~ ws ~ trim_r? ~ "#}" }
if_close = { "{#" ~ trim_l? ~ ws ~ "endif" ~ ws ~ trim_r? ~ "#}" }

elif_branch = {
    "{#" ~ trim_l? ~ ws ~ "elif" ~ ws ~ expr ~ ws ~ trim_r? ~ "#}" ~ node*
}

else_branch = {
    "{#" ~ trim_l? ~ ws ~ "else" ~ ws ~ trim_r? ~ "#}" ~ node*
}

foreach_block = {
    foreach_open ~ node* ~ foreach_close
}

foreach_open  = { "{#" ~ trim_l? ~ ws ~ "foreach" ~ ws ~ identifier ~ ws ~ "in" ~ ws ~ expr ~ ws ~ trim_r? ~ "#}" }
foreach_close = { "{#" ~ trim_l? ~ ws ~ "endforeach" ~ ws ~ trim_r? ~ "#}" }

// ── Expressions ─────────────────────────────────────────────────────────
// Used inside if conditions, processor values, command args, foreach iterables.
// This layer is whitespace-insensitive (ws between tokens).

expr = { unary_expr ~ (ws ~ bin_op ~ ws ~ unary_expr)* }

unary_expr = {
    unary_op ~ ws ~ atom
  | atom
}

atom = {
    "(" ~ ws ~ expr ~ ws ~ ")"
  | array_literal
  | variable
  | processor_call
  | command_call
  | trigger
  | document_ref
  | bool_literal
  | none_literal
  | number
  | quoted_string
}

array_literal = { "[" ~ ws ~ (expr ~ (ws ~ "," ~ ws ~ expr)*)? ~ ws ~ "]" }

// ── Operators ───────────────────────────────────────────────────────────

bin_op = {
    "=="
  | "!="
  | "<="
  | ">="
  | "<"
  | ">"
  | "&&"
  | "||"
  | "+"
  | "-"
  | "*"
  | "/"
  | "%"
}

unary_op = { "!" | "-" }

// ── Literals & atoms ────────────────────────────────────────────────────

bool_literal = @{ "true" | "false" }
none_literal = @{ "none" }

number = @{
    "-"? ~ ASCII_DIGIT+ ~ ("." ~ ASCII_DIGIT+)?
}

/// Double-quoted string with escape sequences
quoted_string = ${ "\"" ~ string_inner ~ "\"" }
string_inner  = @{ (escape_seq | !("\"" | "\\") ~ ANY)* }
escape_seq    = @{ "\\" ~ ("\"" | "\\" | "n" | "t" | "r") }

// ── Identifiers ─────────────────────────────────────────────────────────

identifier = @{
    (ASCII_ALPHA | "_") ~ (ASCII_ALPHANUMERIC | "_")*
}

/// Dotted name for namespaced processors: "core.weaver.rng"
dotted_name = @{
    identifier ~ ("." ~ identifier)*
}

// ── Trim markers ────────────────────────────────────────────────────────
//
// A `-` immediately inside a delimiter eats the whitespace on that side:
// `{#- if x -#}`, `{{- name -}}`, `$[- cmd() -]`. Left and right are
// separate rules so a lone marker can be told apart from its position in
// the pair tree.
//
// There is no ambiguity with unary minus or subtraction. `{{-x}}` holds an
// identifier, never an expression. In `{# if x -#}` pest's `expr` tries to
// consume `- <atom>`, fails at `#}`, and backtracks the repetition — so
// `{# if x - 1 -#}` parses as `x - 1` with a trailing marker, as written.

trim_l = { "-" }
trim_r = { "-" }

// ── Whitespace (explicit, not implicit) ─────────────────────────────────
// We do NOT use pest's implicit WHITESPACE rule because template-level
// text is whitespace-significant. Instead we use `ws` explicitly
// inside expression contexts.

ws = _{ (" " | "\t" | "\r" | "\n")* }
